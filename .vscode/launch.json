{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (cl.exe build)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${fileDirname}\\..\\build\\app.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}\\..",
            "environment": [],
            "preLaunchTask": "build (cl)"
        }
    ]
}
